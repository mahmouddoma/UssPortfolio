import { AppInViewportDirective } from './app-in-viewport.directive';

describe('AppInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new AppInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
